// primer parcial.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
#include "math.h"
#include "conio.h"

using namespace std;

void main () 
{
	float s, c, x;
	int n, i;
	cout << "Ingrese el numero a calcular" << endl;
	cin >> c;
	cout << "Ingrese la cantidad de veces a calcular " << endl;
	cin >> n;
	x=1; s=0;
	for (i=1; i<=n; i++) {
		s=s+ (pow(c, i)*x);
		cout << pow(c,i) << ", " ;
		x=x*-1;
	}
	cout << endl;
	cout << "La suma total es " << s << endl;
	getch () ;

}
